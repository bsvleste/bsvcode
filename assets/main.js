/* import { el, Fragment, render } from '../lib/dom.js'
import { Header } from './components/Header.js'

const $app = document.querySelector('#app')

$app.appendChild(render(el('h1', 'Inciando o projeto')))
$app.appendChild(
  render(el('div', [el('h3', { style: 'color:red' }, 'Segunda parte')]))
)
$app.appendChild(render(el(Header)))
 */
